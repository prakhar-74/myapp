{
    "name": "myapp",
    "version": "1.0.0",
    "description": "Node.js app for Jenkins HD pipeline",
    "main": "src/server.js",
    "scripts": {
        "start": "node src/server.js",
        "build": "echo \"Nothing to build for Node API\"",
        "test": "jest --runInBand --coverage",
        "lint": "echo \"(optional ESLint)\""
    },
    "dependencies": {
        "bcryptjs": "^2.4.3",
        "express": "^4.19.2",
        "jsonwebtoken": "^9.0.2",
        "morgan": "^1.10.0",
        "prom-client": "^15.1.3"
    },
    "devDependencies": {
        "jest": "^29.7.0",
        "supertest": "^7.0.0"
    }
}